#######################################################################################################################
## Locks - Lock All
#######################################################################################################################
locks_lock_all:
  alias: Lock All Locks
  description: Lock all door locks.
  icon: mdi:lock
  mode: restart
  sequence:
    - service: lock.lock #VIRTUAL
      entity_id: all