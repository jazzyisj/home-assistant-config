#######################################################################################################################
## Media Play
#######################################################################################################################
test: #TEST
  alias: "Test"
  sequence:
    - variables:
        media_on_players: "{{ expand(state_attr('sensor.radio_media_player','players')) }}"

    - service: system_log.write
      data:
        message:  "{{ media_on_players }}"

    - service: system_log.write
      data:
        message:  "{{ media_on_players|map(attribute='entity_id')|list }}"


