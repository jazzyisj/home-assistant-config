variable_test: #TEST
  fields:
    test:
      description: 'Test'
  variables:
    test: >
      {% set test2 = test|default('default') %}
      {{ test2 }}
  sequence:
    - service: system_log.write
      data:
        message: 'Variable: {{ test }}'


