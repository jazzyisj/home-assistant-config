|
  {%- if states.sensor.offline_devices.attributes.entities is defined -%}
  {%- if states('sensor.offline_devices')|int == 0 -%}
  No offline devices.
  {%- else -%}
  {%- for device in state_attr('sensor.offline_devices','entities').split(',') -%}
  - {{ state_attr(device,'friendly_name') ~ '\n' }}
  {%- endfor -%}
  {%- endif -%}
  {%- else -%}
  Error
  {%- endif -%}