- binary_sensor:
    - unique_id: google_location_connected
      name: Google Location Connected # must check source_type also, state will be retained even when not connected
      icon: "{{ 'mdi:google-maps' if is_state('binary_sensor.google_location_connected','on') else 'mdi:alert-circle' }}"
      device_class: connectivity
      state: >
        {{ not states('device_tracker.google_maps_101131226839468750203')|lower in ['unknown','unavailable','none']
            and not state_attr('device_tracker.google_maps_101131226839468750203','source_type')|lower in ['','none']
            and not states('device_tracker.google_maps_115097751563829374630')|lower in ['unknown','unavailable','none']
            and not state_attr('device_tracker.google_maps_115097751563829374630','source_type')|lower in ['','none'] }}

    - unique_id: google_location_connected_alert
      name: Google Location Connected Alert
      icon: mdi:google-maps
      state: "{{ is_state('binary_sensor.google_location_connected','off') and is_state('input_boolean.presence_alerts','on') }}"

    - unique_id: unknown_network_device
      state: "{{ states('sensor.unknown_network_devices')|int > 0  and is_state('input_boolean.presence_alerts','on') }}"

    - unique_id: bluetooth_alert
      name: Bluetooth Alert #ISSUE this doesn't really detect if bluetooth working, not working
      icon: mdi:bluetooth-off
      state: >
        {{ state_attr('device_tracker.jlaptop_bt','source_type')|lower == 'none'
            and state_attr('device_tracker.jphone_bt','source_type')|lower == 'none'
            and state_attr('device_tracker.sphone_bt','source_type')|lower == 'none'
            and state_attr('device_tracker.stablet_bt','source_type')|lower == 'none' }}

- sensor:
    - unique_id: unknown_network_devices
      name: Unknown Network Devices
      icon: mdi:devices
      unit_of_measurement: devices
      state: "{{ state_attr('sensor.unknown_network_devices','online') }}"
      attributes:
        online: "{{ states.device_tracker|selectattr('attributes.device_type','eq',null)|selectattr('state','eq','home')|list|count }}"
        total: "{{ states.device_tracker|selectattr('attributes.device_type','eq',null)|list|count }}"
        entities: "{{ states.device_tracker|selectattr('attributes.device_type','eq',null)|map(attribute='entity_id')|list }}"

    - unique_id: wireless_network_devices
      name: Wireless Network Devices
      icon: mdi:access-point-network
      unit_of_measurement: devices
      state: "{{ state_attr('sensor.wireless_network_devices','online') }}"
      attributes:
        online: "{{ states.device_tracker|selectattr('attributes.device_type','eq','wireless')|selectattr('state','eq','home')|list|count }}"
        total: "{{ states.device_tracker|selectattr('attributes.device_type','eq','wireless')|list|count }}"
        entities: "{{ states.device_tracker|selectattr('attributes.device_type','eq','wireless')|map(attribute='entity_id')|list }}"

    - unique_id: wired_network_devices
      name: Wired Network Devices
      icon: mdi:router-network
      unit_of_measurement: devices
      state: "{{ state_attr('sensor.wired_network_devices','online') }}"
      attributes:
        online: "{{ states.device_tracker|selectattr('attributes.device_type','eq','wired')|selectattr('state','eq','home')|list|count }}"
        total: "{{ states.device_tracker|selectattr('attributes.device_type','eq','wired')|list|count }}"
        entities: "{{ states.device_tracker|selectattr('attributes.device_type','eq','wired')|map(attribute='entity_id')|list }}"

    - unique_id: bluetooth_devices
      name: Bluetooth Devices
      icon: mdi:bluetooth-settings
      unit_of_measurement: devices
      state: "{{ state_attr('sensor.bluetooth_devices','online') }}"
      attributes:
        online: "{{ states.device_tracker|selectattr('attributes.device_type','eq','bluetooth')|selectattr('state','eq','home')|list|count }}"
        total: "{{ states.device_tracker|selectattr('attributes.device_type','eq','bluetooth')|list|count }}"
        entities: "{{ states.device_tracker|selectattr('attributes.device_type','eq','bluetooth')|map(attribute='entity_id')|list }}"
