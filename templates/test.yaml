# template:
#   - sensor:
#       - name: 'Sensor Uptime'
#         state: >
#           {% set uptime = states('sensor.rt_ac68p_9168_uptime')|int(-1) -%}
#           {% if uptime >= 60 %}
#             {%- set TIME_MAP = {
#                 'week': (uptime / 604800) % 604800,
#                 'day': (uptime / 86400) % 7,
#                 'hour': (uptime / 3600) % 24,
#                 'minute': (uptime / 60) % 60 } %}
#             {%- for unit, duration in TIME_MAP.items() if duration >= 1 %}
#               {%- if not loop.first %}{{ sep }}{%- endif %}
#               {{- (duration | string).split('.')[0] }} {{ unit }}
#               {%- if duration >= 2 %}s{% endif %}
#             {%- endfor -%}
#           {% else %}
#             just now
#           {% endif %}
