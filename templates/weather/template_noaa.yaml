###############################################################################
## Templates - NOAA
###############################################################################
- binary_sensor:
    - name: 'NOAA Connected'
      unique_id: noaa_connected
      icon: "{{ 'mdi:weather-hurricane' if is_state('binary_sensor.noaa_connected','on') else 'mdi:alert-circle' }}"
      device_class: connectivity
      state: "{{ not states('sensor.noaa_alerts_miz076')|lower in ['unknown','unavailable','none'] }}"

    - name: 'NOAA Connected Alert'
      unique_id: noaa_connected_alert
      icon: mdi:weather-hurricane
      state: >
        {{ not is_state('binary_sensor.noaa_connected','on')
            and is_state('input_boolean.weather_alerts','on')
            and is_state('input_boolean.startup_pending','off') }}

    - name: 'NWS Connected'
      unique_id: nws_connected
      icon: mdi:weather-partly-snowy-rainy
      device_class: connectivity
      state: "{{ not states('weather.kdtw_daynight')|lower in ['unknown','unavailable','none'] }}"

    - name: 'NWS Connected Alert'
      unique_id: nws_connected_alert
      icon: mdi:weather-partly-snowy-rainy
      state: >
        {{ not is_state('binary_sensor.nws_connected','on')
            and is_state('input_boolean.weather_alerts','on')
            and is_state('input_boolean.startup_pending','off') }}

    - name: 'NOAA Weather Alert'
      unique_id: noaa_weather_alert
      icon: mdi:weather-hurricane
      device_class: safety
      state: >
        {{ states('sensor.noaa_alerts_miz076')|int(0) > 0
            and is_state('input_boolean.weather_alerts','on')
            and is_state('input_boolean.startup_pending','off') }}
      attributes:
        type: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {{ state_attr('sensor.noaa_alerts_miz076','alerts')[0].messageType }}
          {% endif %}
        title: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {{ state_attr('sensor.noaa_alerts_miz076','alerts')[0].event|title }}
          {% endif %}
        severity: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {{ state_attr('sensor.noaa_alerts_miz076','alerts')[0].severity|lower }}
          {% endif %}
        condition: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {{ state_attr('sensor.noaa_alerts_miz076','alerts')[0].event }}
          {% endif %}
        description: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {% set headline = state_attr('sensor.noaa_alerts_miz076','alerts')[0].parameters.NWSheadline %}
            {% set inst = state_attr('sensor.noaa_alerts_miz076','alerts')[0].instruction %}
            {{ headline[0] ~ '.' if headline != None }}
            {{ inst ~ '.' if inst != None }}
          {% endif %}
        starts: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {% if state_attr('sensor.noaa_alerts_miz076','alerts')[0].onset|lower  not in ['','none'] %}
              {{ state_attr('sensor.noaa_alerts_miz076','alerts')[0].onset|as_datetime|as_local }}
            {% endif %}
          {% endif %}
        ends: >
          {% if state_attr('sensor.noaa_alerts_miz076','alerts')|lower not in ['','[]','unknown','unavailable','none'] %}
            {% if state_attr('sensor.noaa_alerts_miz076','alerts')[0].expires|lower  not in ['','none'] %}
              {{ state_attr('sensor.noaa_alerts_miz076','alerts')[0].expires|as_datetime|as_local }}
            {% endif %}
          {% endif %}
        starts_text: >
          {% if state_attr('binary_sensor.noaa_weather_alert','starts')|lower not in ['','unknown','unavailable','none'] %}
            {% set starts = state_attr('binary_sensor.noaa_weather_alert','starts')|as_datetime|as_local %}
            {% set today = starts.day == now().day %}
            {% set tomorrow = starts.day == (starts + timedelta(days=1)).day %}
            {% set begins = 'today' if today else 'tomorrow' if tomorrow else starts.strftime('%A') %}
            {{ begins ~ ' at ' ~ starts.strftime('%-I:%M %p') }}
          {% endif %}
        ends_text: >
          {% if state_attr('binary_sensor.noaa_weather_alert','ends')|lower not in ['','unknown','unavailable','none'] %}
            {% set ends = state_attr('binary_sensor.noaa_weather_alert','ends')|as_datetime|as_local %}
            {% set today = ends.day == now().day %}
            {% set tomorrow = ends.day == (ends + timedelta(days=1)).day %}
            {% set until = 'today' if today else 'tomorrow' if tomorrow else ends.strftime('%A') %}
            {{ until ~ ' at ' ~ ends.strftime('%-I:%M %p') }}
          {% endif %}
        active: >
          {% if state_attr('binary_sensor.noaa_weather_alert','starts')|lower not in ['','unknown','unavailable','none'] %}
            {{ now() > state_attr('binary_sensor.noaa_weather_alert','starts')|as_datetime }}
          {% endif %}
        availability: "{{ is_state('binary_sensor.noaa_connected','on') }}"

- sensor:
    - name: 'KDTW Apparent Temperature'
      unique_id: kdtw_apparent_temperature
      icon: mdi:thermometer
      device_class: temperature
      state_class: measurement
      unit_of_measurement: '°C'
      state: >
        {% if is_number(states('sensor.kdtw_heat_index')) %} {{ states('sensor.kdtw_heat_index')|int }}
        {% elif is_number(states('sensor.kdtw_wind_chill')) %} {{ states('sensor.kdtw_wind_chill')|int }}
        {% elif is_number(states('sensor.kdtw_temperature')) %} {{ states('sensor.kdtw_temperature')|int }}
        {% endif %}
      availability: >
        {{ is_number(states('sensor.kdtw_heat_index'))
            or is_number(states('sensor.kdtw_wind_chill'))
            or is_number(states('sensor.kdtw_temperature')) }}

    - name: 'KDTW Precipitation Probability'
      unique_id: kdtw_precipitation_probability
      state_class: measurement
      unit_of_measurement: '%'
      state: >
        {% if state_attr('weather.kdtw_hourly','forecast')[0] is defined
            and is_number(state_attr('weather.kdtw_hourly','forecast')[0]['precipitation_probability']) %}
          {{ state_attr('weather.kdtw_hourly','forecast')[0]['precipitation_probability']|int }}
        {% endif %}
      availability: >
        {{ state_attr('weather.kdtw_hourly','forecast')[0] is defined
            and is_number(state_attr('weather.kdtw_hourly','forecast')[0].precipitation_probability) }}
