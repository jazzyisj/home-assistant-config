###############################################################################
## Template - Sensor
###############################################################################
- binary_sensor:
    - name: 'Indoor Sump Active'
      unique_id: indoor_sump_active
      icon: mdi:water-pump
      state: "{{ states('sensor.indoor_sump_power')|int(-1) > 10 }}"
