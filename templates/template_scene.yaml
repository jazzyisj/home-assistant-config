###############################################################################
## Template - Scene
###############################################################################
- binary_sensor:
    - name: 'Scene Active'
      unique_id: scene_active
      icon: >
        {% if is_state('input_boolean.movie_scene','on') %}
            {{ states.input_boolean.movie_scene.attributes.icon }}
        {% elif is_state('input_boolean.company_scene','on') %}
            {{ states.input_boolean.company_scene.attributes.icon }}
        {% elif is_state('input_boolean.chill_scene','on') %}
            {{ states.input_boolean.chill_scene.attributes.icon }}
        {% else %} mdi:movie-filter
        {% endif %}
      state: >
        {{ is_state('input_boolean.chill_scene','on')
            or is_state('input_boolean.company_scene','on')
            or is_state('input_boolean.movie_scene','on') }}

    - name: 'Scene LED Alert'
      unique_id: scene_led_alert
      state: >
        {{ is_state('binary_sensor.waketime_active','on')
            or is_state('binary_sensor.bedtime_active','on')
            or is_state('input_boolean.bedtime_delayed','on') }}
