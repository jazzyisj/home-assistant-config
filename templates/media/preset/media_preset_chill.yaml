###############################################################################
## Media Speaker Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: group.media_preset_media_players
      attribute: entity_id
      to:
      for: 30

    - trigger: state
      entity_id: input_text.media_speaker_chill
      to:
  select:
    - name: "Media Speaker Chill"
      unique_id: media_speaker_chill
      state: "{{ states('input_text.media_speaker_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_chill
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | list | sort }}

###############################################################################
## Media Video Display Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: group.cast_media_players
      attribute: entity_id
      to:
      for: 30

    - trigger: state
      entity_id: input_text.media_video_display_chill
      to:
  select:
    - name: "Media Video Display Chill"
      unique_id: media_video_display_chill
      state: "{{ states('input_text.media_video_display_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_chill
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | list | sort }}

###############################################################################
## Media Artist Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id:
        - input_text.media_artist_chill
        - button.update_mass_media
      to:
  action:
    - action: mass.get_library
      data:
        favorite: true
        limit: 50
        media_type: artist
      response_variable: mass_artists

    - variables:
        artists: >
          {% set artist_list = namespace(names=[]) %}
          {% for artist in mass_artists['artists'] %}
            {% set artist_list.names = artist_list.names + [artist.name] %}
          {% endfor %}
          {{ artist_list.names | unique | list }}
  select:
    - name: "Media Artist Chill"
      unique_id: media_artist_chill
      state: "{{ states('input_text.media_artist_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_chill
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

###############################################################################
## Media Artist Filter Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id:
        - input_select.media_type_chill
        - input_text.media_artist_filter_chill
        - select.media_track_chill
        - select.media_album_chill
        - select.media_album_filter_chill
        - button.update_mass_media
      to:
      not_from:
        - unknown
        - unavailable
  action:
    - action: mass.get_library
      data:
        media_type: track
      response_variable: mass_tracks

    - action: mass.get_library
      data:
        media_type: album
      response_variable: mass_albums

    - variables:
        artists: >
          {% set artist_list = namespace(names=['']) %}
          {% if states('input_select.media_type_chill') == 'Track' %}
            {% for track in mass_tracks['tracks'] %}
              {% if track.name == states('select.media_track_chill')
                  and states('select.media_album_filter_chill')
                    in ['', 'unknown', track.album.name] %}
                {% set artist_list.names = artist_list.names + [track.artists[0].name] %}
              {% endif %}
            {% endfor %}
          {% elif states('input_select.media_type_chill') == 'Album' %}
            {% for album in mass_albums['albums'] %}
              {% if album.name == states('select.media_album_chill') %}
                {% set artist_list.names = artist_list.names + [album.artists[0].name] %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {{ artist_list.names | unique | list }}
  select:
    - name: "Media Artist Filter Chill"
      unique_id: media_artist_filter_chill
      state: "{{ states('input_text.media_artist_filter_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_chill
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

###############################################################################
## Media Album Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id:
        - input_text.media_album_chill
        - button.update_mass_media
      to:
  action:
    - action: mass.get_library
      data:
        media_type: album
      response_variable: mass_albums

    - variables:
        albums: >
          {% set album_list = namespace(names=[]) %}
          {% for album in mass_albums['albums'] %}
            {% set album_list.names = album_list.names + [album.name] %}
          {% endfor %}
          {{ album_list.names | unique | list }}
  select:
    - name: "Media Album Chill"
      unique_id: media_album_chill
      state: "{{ states('input_text.media_album_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_chill
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

###############################################################################
## Media Album Filter Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id:
        - input_select.media_type_chill
        - input_text.media_album_filter_chill
        - select.media_track_chill
        - select.media_artist_filter_chill
        - button.update_mass_media
      to:
      not_from:
        - unknown
        - unavailable
  action:
    - action: mass.get_library
      data:
        media_type: track
      response_variable: mass_tracks

    - variables:
        albums: >
          {% set album_list = namespace(names=['']) %}
          {% if states('input_select.media_type_chill') == 'Track' %}
            {% for track in mass_tracks['tracks'] %}
              {% if track.name == states('select.media_track_chill')
                  and states('select.media_artist_filter_chill')
                    in ['', 'unknown', track.artists[0].name] %}
                {% set album_list.names = album_list.names + [track.album.name] %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {{ album_list.names | unique | list }}
  select:
    - name: "Media Album Filter Chill"
      unique_id: media_album_filter_chill
      state: "{{ states('input_text.media_album_filter_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_chill
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

###############################################################################
## Media Track Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: input_text.media_track_chill
      to:

    - trigger: state
      entity_id: button.update_mass_media
      to:
  action:
    - action: mass.get_library
      data:
        media_type: track
      response_variable: mass_tracks

    - variables:
        tracks: >
          {% set track_list = namespace(names=[]) %}
          {% for track in mass_tracks['tracks'] %}
            {% set track_list.names = track_list.names + [track.name] %}
          {% endfor %}
          {{ track_list.names | unique | list }}
  select:
    - name: "Media Track Chill"
      unique_id: media_track_chill
      state: "{{ states('input_text.media_track_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_chill
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

###############################################################################
## Media Playlist Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: input_text.media_playlist_chill
      to:

    - trigger: state
      entity_id: button.update_mass_media
      to:
  action:
    - action: mass.get_library
      data:
        media_type: playlist
      response_variable: mass_playlists

    - variables:
        playlists: >
          {% set playlist_list = namespace(names=[]) %}
          {% for playlist in mass_playlists['playlists'] %}
            {% set playlist_list.names = playlist_list.names + [playlist.name] %}
          {% endfor %}
          {{ playlist_list.names | unique | list }}
  select:
    - name: "Media Playlist Chill"
      unique_id: media_playlist_chill
      state: "{{ states('input_text.media_playlist_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_chill
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

###############################################################################
## Media Radio Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: input_text.media_radio_chill
      to:

    - trigger: state
      entity_id: button.update_mass_media
      to:
  action:
    - action: mass.get_library
      data:
        media_type: radio
      response_variable: mass_radios

    - variables:
        radios: >
          {% set radio_list = namespace(names=[]) %}
          {% for radio in mass_radios['radios'] %}
            {% set radio_list.names = radio_list.names + [radio.name] %}
          {% endfor %}
          {{ radio_list.names | unique | list }}
  select:
    - name: "Media Radio Chill"
      unique_id: media_radio_chill
      state: "{{ states('input_text.media_radio_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_chill
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

###############################################################################
## Media Video Chill
###############################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: input_text.media_video_chill
      to:

    - trigger: state
      entity_id: button.update_mass_media
      to:
  select:
    - name: "Media Video Chill"
      unique_id: media_video_chill
      state: "{{ states('input_text.media_video_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_chill
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml
