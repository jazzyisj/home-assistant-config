###################################################################################################
## Update MASS Media Button
###################################################################################################
- button:
    - name: "Update MASS Media"
      unique_id: udpate_mass_media
      icon: mdi:multimedia
      press: []

###################################################################################################
## Media Speaker
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: group.media_preset_media_players
      attribute: entity_id
      to:
      for: 30

    - trigger: state
      entity_id:
        - input_text.media_speaker_charlie
        - input_text.media_speaker_chill
        - input_text.media_speaker_company
        - input_text.media_speaker_guest
        - input_text.media_speaker_jason
        - input_text.media_speaker_morning
        - input_text.media_speaker_play
        - input_text.media_speaker_shower
        - input_text.media_speaker_sleep
        - input_text.media_speaker_wake
  condition:
    - condition: template
      value_template: >
        {{ state_attr('group.media_preset_media_players', 'entity_id')
            not in ['', '[]', none] }}
  select:
    - name: "Media Speaker Charlie"
      unique_id: media_speaker_charlie
      state: "{{ states('input_text.media_speaker_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_charlie
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Chill"
      unique_id: media_speaker_chill
      state: "{{ states('input_text.media_speaker_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_chill
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Company"
      unique_id: media_speaker_company
      state: "{{ states('input_text.media_speaker_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_company
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Guest"
      unique_id: media_speaker_guest
      state: "{{ states('input_text.media_speaker_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_guest
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Jason"
      unique_id: media_speaker_jason
      state: "{{ states('input_text.media_speaker_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_jason
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Morning"
      unique_id: media_speaker_morning
      state: "{{ states('input_text.media_speaker_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_morning
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Play"
      unique_id: media_speaker_play
      state: "{{ states('input_text.media_speaker_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_play
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Shower"
      unique_id: media_speaker_shower
      state: "{{ states('input_text.media_speaker_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_shower
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Sleep"
      unique_id: media_speaker_sleep
      state: "{{ states('input_text.media_speaker_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_sleep
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Speaker Wake"
      unique_id: media_speaker_wake
      state: "{{ states('input_text.media_speaker_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_speaker_wake
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.media_preset_media_players')
            | map(attribute='name') | sort }}

###################################################################################################
## Media Artist
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: button.update_mass_media
      to:

    - trigger: state
      entity_id:
        - input_text.media_artist_charlie
        - input_text.media_artist_chill
        - input_text.media_artist_company
        - input_text.media_artist_guest
        - input_text.media_artist_jason
        - input_text.media_artist_morning
        - input_text.media_artist_play
        - input_text.media_artist_shower
        - input_text.media_artist_sleep
        - input_text.media_artist_wake

    - trigger: time_pattern
      minutes: 55 # sync with library every hour at 55 min after
  action:
    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: artist
        favorite: true
        limit: 50
      response_variable: mass_artists

    - variables:
        artists: >
          {% set artist_list = namespace(names=[]) %}
          {% for artist in mass_artists['items'] %}
            {% set artist_list.names = artist_list.names + [artist.name] %}
          {% endfor %}
          {{ artist_list.names | unique | list }}
  select:
    - name: "Media Artist Charlie"
      unique_id: media_artist_charlie
      state: "{{ states('input_text.media_artist_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_charlie
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Chill"
      unique_id: media_artist_chill
      state: "{{ states('input_text.media_artist_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_chill
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Company"
      unique_id: media_artist_company
      state: "{{ states('input_text.media_artist_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_company
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Guest"
      unique_id: media_artist_guest
      state: "{{ states('input_text.media_artist_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_guest
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Jason"
      unique_id: media_artist_jason
      state: "{{ states('input_text.media_artist_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_jason
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Morning"
      unique_id: media_artist_morning
      state: "{{ states('input_text.media_artist_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_morning
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Play"
      unique_id: media_artist_play
      state: "{{ states('input_text.media_artist_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_play
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Shower"
      unique_id: media_artist_shower
      state: "{{ states('input_text.media_artist_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_shower
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Sleep"
      unique_id: media_artist_sleep
      state: "{{ states('input_text.media_artist_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_sleep
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Wake"
      unique_id: media_artist_wake
      state: "{{ states('input_text.media_artist_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_wake
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

###################################################################################################
## Media Album
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: button.update_mass_media
      to:

    - trigger: state
      entity_id:
        - input_text.media_album_charlie
        - input_text.media_album_chill
        - input_text.media_album_company
        - input_text.media_album_guest
        - input_text.media_album_jason
        - input_text.media_album_morning
        - input_text.media_album_play
        - input_text.media_album_shower
        - input_text.media_album_sleep
        - input_text.media_album_wake

    - trigger: time_pattern
      minutes: 55 # sync with library every hour at 55 min after
  action:
    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: album
        favorite: true
        limit: 100
      response_variable: mass_albums

    - variables:
        albums: >
          {% set album_list = namespace(names=[]) %}
          {% for album in mass_albums['items'] %}
            {% set album_list.names = album_list.names + [album.name] %}
          {% endfor %}
          {{ album_list.names | unique | list }}
  select:
    - name: "Media Album Charlie"
      unique_id: media_album_charlie
      state: "{{ states('input_text.media_album_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_charlie
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Chill"
      unique_id: media_album_chill
      state: "{{ states('input_text.media_album_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_chill
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Company"
      unique_id: media_album_company
      state: "{{ states('input_text.media_album_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_company
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Guest"
      unique_id: media_album_guest
      state: "{{ states('input_text.media_album_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_guest
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Jason"
      unique_id: media_album_jason
      state: "{{ states('input_text.media_album_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_jason
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Morning"
      unique_id: media_album_morning
      state: "{{ states('input_text.media_album_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_morning
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Play"
      unique_id: media_album_play
      state: "{{ states('input_text.media_album_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_play
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Shower"
      unique_id: media_album_shower
      state: "{{ states('input_text.media_album_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_shower
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Sleep"
      unique_id: media_album_sleep
      state: "{{ states('input_text.media_album_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_sleep
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Wake"
      unique_id: media_album_wake
      state: "{{ states('input_text.media_album_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_wake
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

###################################################################################################
## Media Track
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: button.update_mass_media
      to:

    - trigger: state
      entity_id:
        - input_text.media_track_charlie
        - input_text.media_track_chill
        - input_text.media_track_company
        - input_text.media_track_guest
        - input_text.media_track_jason
        - input_text.media_track_morning
        - input_text.media_track_play
        - input_text.media_track_shower
        - input_text.media_track_sleep
        - input_text.media_track_wake

    - trigger: time_pattern
      minutes: 55 # sync with library every hour at 55 min after
  action:
    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: track
        favorite: true
        limit: 100
      response_variable: mass_tracks

    - variables:
        tracks: >
          {% set track_list = namespace(names=[]) %}
          {% for track in mass_tracks['items'] %}
            {% set track_list.names = track_list.names + [track.name] %}
          {% endfor %}
          {{ track_list.names | unique | list }}
  select:
    - name: "Media Track Charlie"
      unique_id: media_track_charlie
      state: "{{ states('input_text.media_track_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_charlie
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Chill"
      unique_id: media_track_chill
      state: "{{ states('input_text.media_track_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_chill
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Company"
      unique_id: media_track_company
      state: "{{ states('input_text.media_track_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_company
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Guest"
      unique_id: media_track_guest
      state: "{{ states('input_text.media_track_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_guest
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Jason"
      unique_id: media_track_jason
      state: "{{ states('input_text.media_track_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_jason
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Morning"
      unique_id: media_track_morning
      state: "{{ states('input_text.media_track_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_morning
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Play"
      unique_id: media_track_play
      state: "{{ states('input_text.media_track_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_play
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Shower"
      unique_id: media_track_shower
      state: "{{ states('input_text.media_track_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_shower
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Sleep"
      unique_id: media_track_sleep
      state: "{{ states('input_text.media_track_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_sleep
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

    - name: "Media Track Wake"
      unique_id: media_track_wake
      state: "{{ states('input_text.media_track_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_track_wake
          data:
            value: "{{ option }}"
      options: "{{ tracks }}"

###################################################################################################
## Media Playlist
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: button.update_mass_media
      to:

    - trigger: state
      entity_id:
        - input_text.media_playlist_charlie
        - input_text.media_playlist_chill
        - input_text.media_playlist_company
        - input_text.media_playlist_guest
        - input_text.media_playlist_jason
        - input_text.media_playlist_morning
        - input_text.media_playlist_play
        - input_text.media_playlist_shower
        - input_text.media_playlist_sleep
        - input_text.media_playlist_wake

    - trigger: time_pattern
      minutes: 55 # sync with library every hour at 55 min after
  action:
    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: playlist
        favorite: true
        limit: 100
      response_variable: mass_playlists

    - variables:
        playlists: >
          {% set playlist_list = namespace(names=[]) %}
          {% for playlist in mass_playlists['items'] %}
            {% set playlist_list.names = playlist_list.names + [playlist.name] %}
          {% endfor %}
          {{ playlist_list.names | unique | list }}
  select:
    - name: "Media Playlist Charlie"
      unique_id: media_playlist_charlie
      state: "{{ states('input_text.media_playlist_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_charlie
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Chill"
      unique_id: media_playlist_chill
      state: "{{ states('input_text.media_playlist_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_chill
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Company"
      unique_id: media_playlist_company
      state: "{{ states('input_text.media_playlist_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_company
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Guest"
      unique_id: media_playlist_guest
      state: "{{ states('input_text.media_playlist_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_guest
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Jason"
      unique_id: media_playlist_jason
      state: "{{ states('input_text.media_playlist_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_jason
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Morning"
      unique_id: media_playlist_morning
      state: "{{ states('input_text.media_playlist_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_morning
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Play"
      unique_id: media_playlist_play
      state: "{{ states('input_text.media_playlist_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_play
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Shower"
      unique_id: media_playlist_shower
      state: "{{ states('input_text.media_playlist_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_shower
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Sleep"
      unique_id: media_playlist_sleep
      state: "{{ states('input_text.media_playlist_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_sleep
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

    - name: "Media Playlist Wake"
      unique_id: media_playlist_wake
      state: "{{ states('input_text.media_playlist_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_playlist_wake
          data:
            value: "{{ option }}"
      options: "{{ playlists }}"

###################################################################################################
## Media Radio
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: button.update_mass_media
      to:

    - trigger: state
      entity_id:
        - input_text.media_radio_charlie
        - input_text.media_radio_chill
        - input_text.media_radio_company
        - input_text.media_radio_guest
        - input_text.media_radio_jason
        - input_text.media_radio_morning
        - input_text.media_radio_play
        - input_text.media_radio_shower
        - input_text.media_radio_sleep
        - input_text.media_radio_wake

    - trigger: time_pattern
      minutes: 55 # sync with library every hour at 55 min after
  action:
    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: radio
        favorite: true
        limit: 100
      response_variable: mass_radios

    - variables:
        radios: >
          {% set radio_list = namespace(names=[]) %}
          {% for radio in mass_radios['items'] %}
            {% set radio_list.names = radio_list.names + [radio.name] %}
          {% endfor %}
          {{ radio_list.names | unique | list }}
  select:
    - name: "Media Radio Charlie"
      unique_id: media_radio_charlie
      state: "{{ states('input_text.media_radio_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_charlie
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Chill"
      unique_id: media_radio_chill
      state: "{{ states('input_text.media_radio_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_chill
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Company"
      unique_id: media_radio_company
      state: "{{ states('input_text.media_radio_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_company
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Guest"
      unique_id: media_radio_guest
      state: "{{ states('input_text.media_radio_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_guest
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Jason"
      unique_id: media_radio_jason
      state: "{{ states('input_text.media_radio_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_jason
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Morning"
      unique_id: media_radio_morning
      state: "{{ states('input_text.media_radio_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_morning
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Play"
      unique_id: media_radio_play
      state: "{{ states('input_text.media_radio_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_play
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Shower"
      unique_id: media_radio_shower
      state: "{{ states('input_text.media_radio_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_shower
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Sleep"
      unique_id: media_radio_sleep
      state: "{{ states('input_text.media_radio_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_sleep
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

    - name: "Media Radio Wake"
      unique_id: media_radio_wake
      state: "{{ states('input_text.media_radio_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_radio_wake
          data:
            value: "{{ option }}"
      options: "{{ radios }}"

###################################################################################################
## Media Filters
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: button.update_mass_media
      to:
      not_from:
        - unknown
        - unavailable
  action:
    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: track
        favorite: true
        limit: 100
      response_variable: mass_tracks

    - action: music_assistant.get_library
      data:
        config_entry_id: 01JG86QY33GS4K1FFQJ18SREYP
        media_type: album
        favorite: true
        limit: 100
      response_variable: mass_albums

    - variables:
        albums: >
          {% set album_list = namespace(names=['']) %}
          {% if states('input_select.media_type_chill') == 'Track' %}
            {% for track in mass_tracks['items'] %}
              {% if track.name == states('select.media_track_chill')
                  and states('select.media_artist_filter_chill')
                    in ['', 'unknown', track.artists[0].name] %}
                {% set album_list.names = album_list.names + [track.album.name] %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {{ album_list.names | unique | list }}
        artists: >
          {% set artist_list = namespace(names=['']) %}
          {% if states('input_select.media_type_chill') == 'Track' %}
            {% for track in mass_tracks['items'] %}
              {% if track.name == states('select.media_track_chill')
                  and states('select.media_album_filter_chill')
                    in ['', 'unknown', track.album.name] %}
                {% set artist_list.names = artist_list.names + [track.artists[0].name] %}
              {% endif %}
            {% endfor %}
          {% elif states('input_select.media_type_chill') == 'Album' %}
            {% for album in mass_albums['items'] %}
              {% if album.name == states('select.media_album_chill') %}
                {% set artist_list.names = artist_list.names + [album.artists[0].name] %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {{ artist_list.names | unique | list }}
  select:
###################################################################################################
## Artist Filter
###################################################################################################
    - name: "Media Artist Filter Charlie"
      unique_id: media_artist_filter_charlie
      state: "{{ states('input_text.media_artist_filter_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_charlie
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Chill"
      unique_id: media_artist_filter_chill
      state: "{{ states('input_text.media_artist_filter_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_chill
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Company"
      unique_id: media_artist_filter_company
      state: "{{ states('input_text.media_artist_filter_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_company
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Guest"
      unique_id: media_artist_filter_guest
      state: "{{ states('input_text.media_artist_filter_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_guest
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Jason"
      unique_id: media_artist_filter_jason
      state: "{{ states('input_text.media_artist_filter_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_jason
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Morning"
      unique_id: media_artist_filter_morning
      state: "{{ states('input_text.media_artist_filter_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_morning
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Play"
      unique_id: media_artist_filter_play
      state: "{{ states('input_text.media_artist_filter_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_play
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Shower"
      unique_id: media_artist_filter_shower
      state: "{{ states('input_text.media_artist_filter_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_shower
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Sleep"
      unique_id: media_artist_filter_sleep
      state: "{{ states('input_text.media_artist_filter_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_sleep
          data:
            value: "{{ option }}"
      options: "{{ artists }}"

    - name: "Media Artist Filter Wake"
      unique_id: media_artist_filter_wake
      state: "{{ states('input_text.media_artist_filter_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_artist_filter_wake
          data:
            value: "{{ option }}"
      options: "{{ artists }}"


###################################################################################################
## Album Filter
###################################################################################################
    - name: "Media Album Filter Charlie"
      unique_id: media_album_filter_charlie
      state: "{{ states('input_text.media_album_filter_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_charlie
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Chill"
      unique_id: media_album_filter_chill
      state: "{{ states('input_text.media_album_filter_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_chill
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Company"
      unique_id: media_album_filter_company
      state: "{{ states('input_text.media_album_filter_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_company
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Guest"
      unique_id: media_album_filter_guest
      state: "{{ states('input_text.media_album_filter_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_guest
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Jason"
      unique_id: media_album_filter_jason
      state: "{{ states('input_text.media_album_filter_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_jason
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Morning"
      unique_id: media_album_filter_morning
      state: "{{ states('input_text.media_album_filter_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_morning
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Play"
      unique_id: media_album_filter_play
      state: "{{ states('input_text.media_album_filter_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_play
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Shower"
      unique_id: media_album_filter_shower
      state: "{{ states('input_text.media_album_filter_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_shower
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Sleep"
      unique_id: media_album_filter_sleep
      state: "{{ states('input_text.media_album_filter_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_sleep
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

    - name: "Media Album Filter Wake"
      unique_id: media_album_filter_wake
      state: "{{ states('input_text.media_album_filter_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_album_filter_wake
          data:
            value: "{{ option }}"
      options: "{{ albums }}"

###################################################################################################
## Media Video Display
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id: group.cast_media_players
      attribute: entity_id
      to:
      for: 30

    - trigger: state
      entity_id:
        - input_text.media_video_display_charlie
        - input_text.media_video_display_chill
        - input_text.media_video_display_company
        - input_text.media_video_display_guest
        - input_text.media_video_display_jason
        - input_text.media_video_display_morning
        - input_text.media_video_display_play
        - input_text.media_video_display_shower
        - input_text.media_video_display_sleep
        - input_text.media_video_display_wake
  condition:
    - condition: template
      value_template: >
        {{ state_attr('group.cast_media_players', 'entity_id')
            not in ['', '[]', none] }}
  select:
    - name: "Media Video Display Charlie"
      unique_id: media_video_display_charlie
      state: "{{ states('input_text.media_video_display_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_charlie
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Chill"
      unique_id: media_video_display_chill
      state: "{{ states('input_text.media_video_display_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_chill
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Company"
      unique_id: media_video_display_company
      state: "{{ states('input_text.media_video_display_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_company
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Guest"
      unique_id: media_video_display_guest
      state: "{{ states('input_text.media_video_display_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_guest
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Jason"
      unique_id: media_video_display_jason
      state: "{{ states('input_text.media_video_display_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_jason
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Morning"
      unique_id: media_video_display_morning
      state: "{{ states('input_text.media_video_display_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_morning
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Play"
      unique_id: media_video_display_play
      state: "{{ states('input_text.media_video_display_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_play
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Shower"
      unique_id: media_video_display_shower
      state: "{{ states('input_text.media_video_display_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_shower
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Sleep"
      unique_id: media_video_display_sleep
      state: "{{ states('input_text.media_video_display_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_sleep
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

    - name: "Media Video Display Wake"
      unique_id: media_video_display_wake
      state: "{{ states('input_text.media_video_display_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_display_wake
          data:
            value: "{{ option }}"
      options: >
        {{ expand('group.cast_media_players')
            | map(attribute='name') | sort }}

###################################################################################################
## Media Video
###################################################################################################
- trigger:
    - trigger: homeassistant
      event: start

    - trigger: event
      event_type: event_template_reloaded

    - trigger: state
      entity_id:
        - input_text.media_video_charlie
        - input_text.media_video_company
        - input_text.media_video_guest
        - input_text.media_video_jason
        - input_text.media_video_morning
        - input_text.media_video_play
        - input_text.media_video_shower
        - input_text.media_video_sleep
        - input_text.media_video_wake
      to:

    - trigger: state
      entity_id: button.update_mass_media
      to:
    #DISABLED not required until automatic video list
    # - trigger: time_pattern
    #   minutes: 55 # sync with library every hour at 55 min after
  select:
    - name: "Media Video Charlie"
      unique_id: media_video_charlie
      state: "{{ states('input_text.media_video_charlie') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_charlie
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Chill"
      unique_id: media_video_chill
      state: "{{ states('input_text.media_video_chill') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_chill
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Company"
      unique_id: media_video_company
      state: "{{ states('input_text.media_video_company') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_company
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Guest"
      unique_id: media_video_guest
      state: "{{ states('input_text.media_video_guest') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_guest
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Jason"
      unique_id: media_video_jason
      state: "{{ states('input_text.media_video_jason') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_jason
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Morning"
      unique_id: media_video_morning
      state: "{{ states('input_text.media_video_morning') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_morning
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Play"
      unique_id: media_video_play
      state: "{{ states('input_text.media_video_play') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_play
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Shower"
      unique_id: media_video_shower
      state: "{{ states('input_text.media_video_shower') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_shower
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Sleep"
      unique_id: media_video_sleep
      state: "{{ states('input_text.media_video_sleep') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_sleep
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml

    - name: "Media Video Wake"
      unique_id: media_video_wake
      state: "{{ states('input_text.media_video_wake') }}"
      select_option:
        - condition: template
          value_template: "{{ option not in ['', none] }}"

        - action: input_text.set_value
          target:
            entity_id: input_text.media_video_wake
          data:
            value: "{{ option }}"
      options: !include /config/include/media_preset_videos.yaml
