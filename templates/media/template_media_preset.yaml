###############################################################################
## Templates - Media Preset
###############################################################################
- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_chill
      to: ~
  select:
    - name: 'Media Preset Speaker Chill'
      unique_id: media_preset_speaker_chill
      state: "{{ states('input_text.media_preset_speaker_chill') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_chill
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_company
      to: ~
  select:
    - name: 'Media Preset Speaker Company'
      unique_id: media_preset_speaker_company
      state: "{{ states('input_text.media_preset_speaker_company') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_company
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_guest
      to: ~
  select:
    - name: 'Media Preset Speaker Guest'
      unique_id: media_preset_speaker_guest
      state: "{{ states('input_text.media_preset_speaker_guest') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_guest
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_jason
      to: ~
  select:
    - name: 'Media Preset Speaker Jason'
      unique_id: media_preset_speaker_jason
      state: "{{ states('input_text.media_preset_speaker_jason') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_jason
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_morning
      to: ~
  select:
    - name: 'Media Preset Speaker Morning'
      unique_id: media_preset_speaker_morning
      state: "{{ states('input_text.media_preset_speaker_morning') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_morning
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_sheri
      to: ~
  select:
    - name: 'Media Preset Speaker Sheri'
      unique_id: media_preset_speaker_sheri
      state: "{{ states('input_text.media_preset_speaker_sheri') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_sheri
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_shower
      to: ~
  select:
    - name: 'Media Preset Speaker Shower'
      unique_id: media_preset_speaker_shower
      state: "{{ states('input_text.media_preset_speaker_shower') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_shower
          data:
            value: '{{ option }}'
      options: "{{ ['Bathroom Speaker'] }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_sleep
      to: ~
  select:
    - name: 'Media Preset Speaker Sleep'
      unique_id: media_preset_speaker_sleep
      state: "{{ states('input_text.media_preset_speaker_sleep') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_sleep
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.media_preset_media_players
      attribute: player_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_speaker_wake
      to: ~
  select:
    - name: 'Media Preset Speaker Wake'
      unique_id: media_preset_speaker_wake
      state: "{{ states('input_text.media_preset_speaker_wake') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_speaker_wake
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.media_preset_media_players','player_names') }}"

    ###############################################################################
    ## Media Preset Radio
    ###############################################################################
- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_wake
      to: ~
  select:
    - name: 'Media Preset Radio Wake'
      unique_id: media_preset_radio_wake
      state: "{{ states('input_text.media_preset_radio_wake') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_wake
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_morning
      to: ~
  select:
    - name: 'Media Preset Radio Morning'
      unique_id: media_preset_radio_morning
      state: "{{ states('input_text.media_preset_radio_morning') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_morning
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_sleep
      to: ~
  select:
    - name: 'Media Preset Radio Sleep'
      unique_id: media_preset_radio_sleep
      state: "{{ states('input_text.media_preset_radio_sleep') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_sleep
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_jason
      to: ~
  select:
    - name: 'Media Preset Radio Jason'
      unique_id: media_preset_radio_jason
      state: "{{ states('input_text.media_preset_radio_jason') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_jason
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_sheri
      to: ~
  select:
    - name: 'Media Preset Radio Sheri'
      unique_id: media_preset_radio_sheri
      state: "{{ states('input_text.media_preset_radio_sheri') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_sheri
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_guest
      to: ~
  select:
    - name: 'Media Preset Radio Guest'
      unique_id: media_preset_radio_guest
      state: "{{ states('input_text.media_preset_radio_guest') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_guest
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_shower
      to: ~
  select:
    - name: 'Media Preset Radio Shower'
      unique_id: media_preset_radio_shower
      state: "{{ states('input_text.media_preset_radio_shower') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_shower
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_chill
      to: ~
  select:
    - name: 'Media Preset Radio Chill'
      unique_id: media_preset_radio_chill
      state: "{{ states('input_text.media_preset_radio_chill') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_chill
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.radio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_radio_company
      to: ~
  select:
    - name: 'Media Preset Radio Company'
      unique_id: media_preset_radio_company
      state: "{{ states('input_text.media_preset_radio_company') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_radio_company
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.radio_playlists','playlist_names') }}"

    ###############################################################################
    ## Media Preset Spotify
    ###############################################################################
- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_wake
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Wake'
      unique_id: media_preset_spotify_hassio_wake
      state: "{{ states('input_text.media_preset_spotify_hassio_wake') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_wake
          data:
            value: '{{ option }}'
      options: >
        {% set options = namespace(value=[]) %}
        {% set playlists = state_attr('sensor.playlists_sensor','playlists') %}
        {% for item in playlists -%}
          {% set options.value = options.value + [item.name] %}
        {% endfor %}
        {{ options.value }}

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_morning
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Morning'
      unique_id: media_preset_spotify_hassio_morning
      state: "{{ states('input_text.media_preset_spotify_hassio_morning') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_morning
          data:
            value: '{{ option }}'
      options: >
        {% set options = namespace(value=[]) %}
        {% set playlists = state_attr('sensor.playlists_sensor','playlists') %}
        {% for item in playlists -%}
          {% set options.value = options.value + [item.name] %}
        {% endfor %}
        {{ options.value }}

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_sleep
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Sleep'
      unique_id: media_preset_spotify_hassio_sleep
      state: "{{ states('input_text.media_preset_spotify_hassio_sleep') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_sleep
          data:
            value: '{{ option }}'
      options: >
        {% set options = namespace(value=[]) %}
        {% set playlists = state_attr('sensor.playlists_sensor','playlists') %}
        {% for item in playlists -%}
          {% set options.value = options.value + [item.name] %}
        {% endfor %}
        {{ options.value }}

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_jason
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Jason'
      unique_id: media_preset_spotify_hassio_jason
      state: "{{ states('input_text.media_preset_spotify_hassio_jason') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_jason
          data:
            value: '{{ option }}'
      options: >
        {% set options = namespace(value=[]) %}
        {% set playlists = state_attr('sensor.playlists_sensor','playlists') %}
        {% for item in playlists -%}
          {% set options.value = options.value + [item.name] %}
        {% endfor %}
        {{ options.value }}

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_sheri
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Sheri'
      unique_id: media_preset_spotify_hassio_sheri
      state: "{{ states('input_text.media_preset_spotify_hassio_sheri') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_sheri
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.spotify_hassio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_guest
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Guest'
      unique_id: media_preset_spotify_hassio_guest
      state: "{{ states('input_text.media_preset_spotify_hassio_guest') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_guest
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.spotify_hassio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_shower
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Shower'
      unique_id: media_preset_spotify_hassio_shower
      state: "{{ states('input_text.media_preset_spotify_hassio_shower') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_shower
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.spotify_hassio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_chill
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Chill'
      unique_id: media_preset_spotify_hassio_chill
      state: "{{ states('input_text.media_preset_spotify_hassio_chill') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_chill
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.spotify_hassio_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.spotify_hassio_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_spotify_hassio_company
      to: ~
  select:
    - name: 'Media Preset Spotify Hassio Company'
      unique_id: media_preset_spotify_hassio_company
      state: "{{ states('input_text.media_preset_spotify_hassio_company') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_spotify_hassio_company
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.spotify_hassio_playlists','playlist_names') }}"

    ###############################################################################
    ## Media Preset Youtube
    ###############################################################################
- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_wake
      to: ~
  select:
    - name: 'Media Preset Youtube Wake'
      unique_id: media_preset_youtube_wake
      state: "{{ states('input_text.media_preset_youtube_wake') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_wake
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_morning
      to: ~
  select:
    - name: 'Media Preset Youtube Morning'
      unique_id: media_preset_youtube_morning
      state: "{{ states('input_text.media_preset_youtube_morning') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_morning
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_sleep
      to: ~
  select:
    - name: 'Media Preset Youtube Sleep'
      unique_id: media_preset_youtube_sleep
      state: "{{ states('input_text.media_preset_youtube_sleep') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_sleep
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_jason
      to: ~
  select:
    - name: 'Media Preset Youtube Jason'
      unique_id: media_preset_youtube_jason
      state: "{{ states('input_text.media_preset_youtube_jason') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_jason
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_sheri
      to: ~
  select:
    - name: 'Media Preset Youtube Sheri'
      unique_id: media_preset_youtube_sheri
      state: "{{ states('input_text.media_preset_youtube_sheri') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_sheri
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_guest
      to: ~
  select:
    - name: 'Media Preset Youtube Guest'
      unique_id: media_preset_youtube_guest
      state: "{{ states('input_text.media_preset_youtube_guest') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_guest
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_shower
      to: ~
  select:
    - name: 'Media Preset Youtube Shower'
      unique_id: media_preset_youtube_shower
      state: "{{ states('input_text.media_preset_youtube_shower') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_shower
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_chill
      to: ~
  select:
    - name: 'Media Preset Youtube Chill'
      unique_id: media_preset_youtube_chill
      state: "{{ states('input_text.media_preset_youtube_chill') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_chill
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"

- trigger:
    - platform: state
      entity_id: sensor.youtube_playlists
      attribute: playlist_names
      to: ~

    - platform: state
      entity_id: input_text.media_preset_youtube_company
      to: ~
  select:
    - name: 'Media Preset Youtube Company'
      unique_id: media_preset_youtube_company
      state: "{{ states('input_text.media_preset_youtube_company') }}"
      select_option:
        - condition: template
          value_template: '{{ option != none }}'

        - service: input_text.set_value
          target:
            entity_id: input_text.media_preset_youtube_company
          data:
            value: '{{ option }}'
      options: "{{ state_attr('sensor.youtube_playlists','playlist_names') }}"
