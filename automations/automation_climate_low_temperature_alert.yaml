#######################################################################################################################
## Climate - Indoor Low Temperature Alert
#######################################################################################################################
- id: climate_indoor_low_temperature_alert
  alias: "[Climate] Indoor Low Temperature Alert"
  description: "Send notification when low temperature alert turns on."
  initial_state: true
  mode: single
  trigger:
    - platform: state
      entity_id: alert.climate_indoor_low_temperature
      to: 'on'

  action:
    - repeat:
        sequence:
          - service: script.tts_announcement
            data:
              play_message: >
                  {%- set thresh = states('input_number.low_temperature_threshold')|float -%}
                  {%- set t = '%0.0f'|format(states('sensor.upstairs_thermostat_temperature')|float)|float -%}
                  {%- set b = '%0.0f'|format(states('sensor.upstairs_bathroom_temperature')|float)|float -%}
                  Attention! The indoor low temperature alert is active. The
                  {%- if t < thresh %} current indoor temperature is {{ t }}
                  {%- elif b < thresh %} upstairs bathroom temperature is {{ b }}
                  {%- endif %} degrees.
              quiet_play: true

          # wait for alert to turned off/dismissed
          - wait_template: "{{ not is_state('alert.climate_indoor_low_temperature','on') }}"
            timeout:
              hours: 1

        until:
          - condition: not
            conditions:
              - condition: state
                entity_id: alert.climate_indoor_low_temperature
                state: 'on'

#######################################################################################################################
## Climate - Close Indoor Low Temperature Notifications
#######################################################################################################################
- id: climate_close_indoor_low_temperature_notifications
  alias: "[Climate] Close Indoor Low Temperature Notifications"
  description: "Dismiss notifications on all devices."
  initial_state: true
  mode: single
  max_exceeded: silent
  trigger:
    - platform: state
      entity_id: alert.climate_indoor_low_temperature
      to:
        - idle
        - 'off'

    - platform: event
      event_type: html5_notification.closed
      event_data:
        tag: indoor_low_temperature

    #BUG html5 closed event doesn't work if notification is in tray
    - platform: event
      event_type: html5_notification.clicked
      event_data:
        action: close_indoor_low_temperature

    - platform: event
      event_type: html5_notification.clicked
      event_data:
        action: pause_indoor_low_temperature

    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: close_indoor_low_temperature

    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: pause_indoor_low_temperature

  action:
    - service: script.close_notifications
      data:
        target: mobile_app_jphone
        category: climate
        tag: indoor_low_temperature
        pause: "{% if trigger.event is defined %}{{ trigger.event.data['action'] == 'pause_indoor_low_temperature' }}{% endif %}"