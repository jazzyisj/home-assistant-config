###############################################################################
## Media Player - Volume Override Off
###############################################################################
- id: media_player_volume_override_off #OCC
  alias: '[Media Player] Volume Override Off'
  description: 'Turn volume override off.'
  mode: restart
  trigger:
    - platform: state
      entity_id: input_boolean.volume_override
      to: 'on'
      for:
        minutes: 120

    - platform: state
      entity_id: binary_sensor.quiet_time #TEMPLATE trigger on reload
      to: 'off'
      from: 'on'

    # override on and all speakers media turned off
    - platform: template
      value_template: >
        {{ is_state('input_boolean.volume_override','on')
            and states.media_player|selectattr('attributes.device_class','eq','speaker')
              |selectattr('state','in',['playing','paused'])|list|count == 0
            and is_state('binary_sensor.radio','off')
            and is_state('binary_sensor.spotify','off')
            and is_state('binary_sensor.youtube','off') }}
      for:
        minutes: 5

    - platform: state
      entity_id: input_select.occupancy_mode
      to:
        - Night
        - Away
        - Vacation
  condition:
    - condition: state
      entity_id: binary_sensor.scene_active
      state: 'off'
  action:
    - service: input_boolean.turn_off
      target:
        entity_id: input_boolean.volume_override
