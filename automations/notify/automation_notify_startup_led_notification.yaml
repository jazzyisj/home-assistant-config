###############################################################################
## Notify - Startup LED Notification
###############################################################################
- id: notify_startup_led_notification
  alias: '[Notify] Startup LED Notification'
  description: 'Set LED notification.'
  mode: queued
  trigger:
    - platform: state
      entity_id: input_boolean.startup_pending
      to: 'off'
      from: 'on'
  action:
    - service: script.startup_led
