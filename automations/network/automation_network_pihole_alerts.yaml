###############################################################################
## Network - Enable PiHole
###############################################################################
- id: network_pause_enable_pihole
  alias: '[Network] Enable PiHole'
  description: 'Pause alert.'
  max_exceeded: silent
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: enable_pihole
  action:
    - service: switch.turn_on
      target:
        entity_id: switch.pi_hole_enabled

###############################################################################
## Network - Pause PiHole Disconnected Alert
###############################################################################
- id: network_pause_pihole_disconnected_alert
  alias: '[Network] Pause PiHole Disconnected Alert'
  description: 'Pause alert.'
  max_exceeded: silent
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: pause_pihole_disconnected
  action:
    - service: alert.turn_off
      target:
        entity_id: alert.pihole_disconnected

###############################################################################
## Network - Clear PiHole Disconnected Notifications
###############################################################################
- id: network_clear_pihole_disconnected_notifications
  alias: '[Network] Clear PiHole Disconnected Notifications'
  description: 'Clear notifications.'
  trigger:
    - platform: state
      entity_id: alert.pihole_disconnected
      to: 'off'
  condition: '{{ now() - states.automation.network_pause_pihole_disconnected_alert.attributes.last_triggered > timedelta(seconds=30) }}'
  action:
    - service: notify.jason
      data:
        message: clear_notification
        data:
          tag: pihole_disconnected

###############################################################################
## Network - Pause PiHole Disabled Alert
###############################################################################
- id: network_pause_pihole_disabled_alert
  alias: '[Network] Pause PiHole Disabled Alert'
  description: 'Pause alert.'
  max_exceeded: silent
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: pause_pihole_disabled
  action:
    - service: alert.turn_off
      target:
        entity_id: alert.pihole_disabled

###############################################################################
## Network - Clear PiHole Disabled Notifications
###############################################################################
- id: network_clear_pihole_disabled_notifications
  alias: '[Network] Clear PiHole Disabled Notifications'
  description: 'Clear notifications.'
  trigger:
    - platform: state
      entity_id: alert.pihole_disabled
      to: 'off'
  condition: '{{ now() - states.automation.network_pause_pihole_disabled_alert.attributes.last_triggered > timedelta(seconds=30) }}'
  action:
    - service: notify.jason
      data:
        message: clear_notification
        data:
          tag: pihole_disabled

###############################################################################
## Network - Pause PiHole Update Alert
###############################################################################
- id: network_pause_pihole_udpate_alert
  alias: '[Network] Pause PiHole Update Alert'
  description: 'Pause alert.'
  max_exceeded: silent
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: pause_pihole_disconnected
  action:
    - service: alert.turn_off
      target:
        entity_id: alert.pihole_update

###############################################################################
## Network - Clear PiHole Update Notifications
###############################################################################
- id: network_clear_pihole_update_notifications
  alias: '[Network] Clear PiHole Update Notifications'
  description: 'Clear notifications.'
  trigger:
    - platform: state
      entity_id: alert.pihole_update
      to: 'off'
  condition: '{{ now() - states.automation.network_pause_pihole_udpate_alert.attributes.last_triggered > timedelta(seconds=30) }}'
  action:
    - service: notify.jason
      data:
        message: clear_notification
        data:
          tag: pihole_update
