###############################################################################
## Yardwaste Day Button
###############################################################################
type: custom:button-card
template: menu_button
entity: sensor.next_yardwaste_day
show_state: true
state_display: "[[[ return states['sensor.next_yardwaste_day'].attributes['display'].toUpperCase(); ]]]"
styles:
  icon:
    - color: >
        [[[
          var day = states['sensor.next_yardwaste_day'].attributes['display'];

          if (day == 'Today' || day == 'Tomorrow')
            return 'var(--calendar-yardwaste-color)';
          else
            return 'var(--state-icon-color)';
        ]]]
  card:
    - background-color: >
        [[[
          if (states['binary_sensor.wan'].state == 'off'
                || states['sensor.next_yardwaste_day'].attributes['display'] == 'unknown')
            return 'var(--entity-button-critical-color)';
          else if (states['input_boolean.yardwaste_alert'].state == 'on')
            return 'var(--entity-button-active-color)';
          else
            return 'var(--ha-card-background)';
        ]]]
tap_action:
  action: navigate
  navigation_path: /calendar
double_tap_action:
  action: call-service
  service: input_boolean.toggle
  data:
    entity_id: input_boolean.yardwaste_alert
hold_action:
  action: navigate
  navigation_path: waste
