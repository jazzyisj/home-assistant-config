###############################################################################
## Upstairs Light Controls
###############################################################################
type: entities
title: "Upstairs Lighting"
entities:
  - type: custom:binary-control-button-row
    entity: light.side_entrance_light
    name: "Side Entrance"
    width: 35px
    height: 35px
    customTheme: true
    isOnColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.side_entrance_light
          timeout: 60000
    double_tap_action:
      action: more-info

  - type: custom:light-brightness-preset-row
    entity: light.hallway_potlights
    name: "Hallway Potlights"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 5
    medBrightness: 50
    hiBrightness: 100
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.hallway_potlights
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_main_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.hallway_potlights' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }
  - type: section
    label: "Living Room"

  - type: custom:light-brightness-preset-row
    entity: light.living_room_fan_light
    name: "Living Room Fan Light"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 5
    medBrightness: 100
    hiBrightness: 255
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.living_room_fan_light
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_main_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.living_room_fan_light' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }

  - type: custom:light-brightness-preset-row
    entity: light.living_room_potlights
    name: "Living Room Potlights"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 5
    medBrightness: 40
    hiBrightness: 100
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.living_room_potlights
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_main_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.living_room_potlights' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }

  - type: custom:binary-control-button-row
    entity: light.living_room_cove_light
    name: "L/R Cove Light"
    width: 35px
    height: 35px
    customTheme: true
    isOnColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.living_room_cove_light
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      class: basic_swipe

  - type: section
    label: "Bathroom"

  - type: custom:light-brightness-preset-row
    entity: light.bathroom_vanity_rgb_light
    name: "Vanity Light"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 10
    medBrightness: 80
    hiBrightness: 150
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.bathroom_vanity_rgb_light
          timeout: 60000
    double_tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: entities
            show_header_toggle: false
            entities:
              - type: custom:slider-entity-row
                entity: light.bathroom_vanity_rgb_light
                name: "RGB Master"
                full_row: false
                toggle: true

              - type: custom:slider-entity-row
                entity: light.bathroom_vanity_light_rgb_bulb_1
                name: "RGB Light 1"
                full_row: false
                toggle: true

              - type: custom:slider-entity-row
                entity: light.bathroom_vanity_light_rgb_bulb_2
                name: "RGB Light 2"
                full_row: false
                toggle: true
          timeout: 60000

    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_bathroom_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.bathroom_vanity_light_rgb_bulb_1' in manual
                  and 'light.bathroom_vanity_light_rgb_bulb_2' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }

  - type: custom:light-brightness-preset-row
    entity: light.bathroom_shower_light
    name: "Shower Light"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 30
    medBrightness: 100
    hiBrightness: 255
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.bathroom_shower_light
          timeout: 60000
    double_tap_action:
      action: more-info

  - type: section
    label: "Bedroom"

  - type: custom:light-brightness-preset-row
    entity: light.bedroom_fan_light
    name: "Bedroom Light"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 5
    medBrightness: 80
    hiBrightness: 175
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.bedroom_fan_light
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_bedroom_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.bedroom_fan_light' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }

  - type: custom:light-brightness-preset-row
    entity: light.bedroom_potlights
    name: "Bedroom Potlights"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 10
    medBrightness: 50
    hiBrightness: 120
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.bedroom_potlights
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_bedroom_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.bedroom_potlights' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }

  - type: section
    label: "Office"

  - type: custom:light-brightness-preset-row
    entity: light.office_fan_light
    name: "Office Fan Light"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 5
    medBrightness: 80
    hiBrightness: 175
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.office_fan_light
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_main_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.office_fan_light' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }

  - type: custom:light-brightness-preset-row
    entity: light.office_potlights
    name: "Office Potlights"
    width: 35px
    height: 35px
    customTheme: true
    isOnLowColor: "var(--light-low-color)"
    isOnMedColor: "var(--light-med-color)"
    isOnHiColor: "var(--light-high-color)"
    isOffColor: " var(--light-off-color)"
    buttonInactiveColor: "var(--light-inactive-color)"
    customSetpoints: true
    lowBrightness: 10
    medBrightness: 50
    hiBrightness: 255
    tap_action:
      action: fire-dom-event
      browser_mod:
        service: browser_mod.popup
        data:
          content:
            type: light
            entity: light.office_potlights
          timeout: 60000
    double_tap_action:
      action: more-info
    card_mod:
      style: >
        :host {
          color:
            {% set manual = state_attr('switch.adaptive_lighting_main_lights','manual_control') %}
            {% if manual != none %}
              {% if 'light.office_potlights' in manual %} var(--entity-warning-color)
              {% else %} var(--primary-text-color)
              {% endif %}
            {% else %} var(--primary-text-color)
            {% endif %}
          ;
        }
card_mod:
  class: basic_swipe
