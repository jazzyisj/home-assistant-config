###############################################################################
## Media Preset Guest
###############################################################################
type: entities
title: "Media Preset: Guest"
state_color: true
show_header_toggle: false
entities:
  - entity: input_select.media_preset_type_guest
    name: "Media Type"

  - type: conditional
    conditions:
      - condition: state
        entity: input_select.media_preset_type_guest
        state: Playlist
    row:
      entity: select.media_preset_playlist_guest
      name: "Playlist"

  - type: conditional
    conditions:
      - condition: state
        entity: input_select.media_preset_type_guest
        state: Radio
    row:
      entity: select.media_preset_radio_guest
      name: "Radio Station"

  - entity: select.media_preset_speaker_guest
    name: "Speaker"

  - entity: input_number.media_preset_volume_guest
    name: "Volume"

  - entity: input_boolean.media_preset_enabled_guest
    name: "Auto Play"

  - type: conditional
    conditions:
      - condition: state
        entity: input_select.media_preset_type_guest
        state: Playlist
    row:
      entity: input_boolean.shuffle_guest
    name: "Shuffle Playlist"

  - type: conditional
    conditions:
      - condition: state
        entity: input_select.media_preset_type_guest
        state: Playlist
    row:
      entity: input_boolean.repeat_guest
      name: "Repeat Playlist"

  - type: button
    name: " "
    service: script.media_play
    action_name: "PLAY"
    tap_action:
      action: perform-action
      perform_action: script.media_play
      data:
        preset: guest
