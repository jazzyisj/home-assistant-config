###############################################################################
## Speaker Groups
###############################################################################
type: entities
title: "Speaker Groups"
icon: mdi:monitor-multiple
entities:
  - type: custom:state-switch
    entity: template
    template: "{{ states('sensor.all_speakers') in ['off','alarm_clock','unknown','unavailable'] }}"
    states:
      true:
        type: custom:hui-element
        card_type: entities
        entities:
          - entity: media_player.all_speakers #MASS media_player.mass_all_speakers
        card_mod:
          class: sub_card
      false:
        type: custom:hui-element
        card_type: media-control
        entity: media_player.all_speakers #MASS media_player.mass_all_speakers

  - type: custom:state-switch
    entity: template
    template: "{{ states('sensor.broadcast_speakers') in ['off','alarm_clock','unknown','unavailable'] }}"
    states:
      true:
        type: custom:hui-element
        card_type: entities
        entities:
          - entity: media_player.broadcast_speakers
        card_mod:
          class: sub_card
      false:
        type: custom:hui-element
        card_type: media-control
        entity: media_player.broadcast_speakers

  - type: custom:state-switch
    entity: template
    template: "{{ states('sensor.inside_speakers') in ['off','alarm_clock','unknown','unavailable'] }}"
    states:
      true:
        type: custom:hui-element
        card_type: entities
        entities:
          - entity: media_player.inside_speakers
        card_mod:
          class: sub_card
      false:
        type: custom:hui-element
        card_type: media-control
        entity: media_player.inside_speakers

  - type: custom:state-switch
    entity: template
    template: "{{ states('sensor.music_speakers') in ['off','alarm_clock','unknown','unavailable'] }}"
    states:
      true:
        type: custom:hui-element
        card_type: entities
        entities:
          - entity: media_player.music_speakers
        card_mod:
          class: sub_card
      false:
        type: custom:hui-element
        card_type: media-control
        entity: media_player.music_speakers

  - type: custom:state-switch
    entity: template
    template: "{{ states('sensor.night_speakers') in ['off','alarm_clock','unknown','unavailable'] }}"
    states:
      true:
        type: custom:hui-element
        card_type: entities
        entities:
          - entity: media_player.night_speakers
        card_mod:
          class: sub_card
      false:
        type: custom:hui-element
        card_type: media-control
        entity: media_player.night_speakers

  - type: custom:state-switch
    entity: template
    template: "{{ states('sensor.quiet_speakers') in ['off','alarm_clock','unknown','unavailable'] }}"
    states:
      true:
        type: custom:hui-element
        card_type: entities
        entities:
          - entity: media_player.quiet_speakers
        card_mod:
          class: sub_card
      false:
        type: custom:hui-element
        card_type: media-control
        entity: media_player.quiet_speakers
