script:
  test:
    sequence:
      - service: browser_mod.navigate
        data:
          path: /lovelace/weather

automation:
  - id: "test"
    trigger:
      - platform: state
        entity_id: input_button.test
    action:
      - service: browser_mod.navigate
        data:
          path: /hacs/entry
