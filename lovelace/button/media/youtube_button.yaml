###############################################################################
## Youtube Button
###############################################################################
type: custom:button-card
template: menu_button
entity: binary_sensor.youtube
name: 'Youtube'
styles:
  icon:
    - color: >
        [[[
          var player_state = states['media_player.youtube'].state;
          var sensor_state = states['binary_sensor.youtube'].state;

          if (states['switch.system_mute'].state == 'on')
            return 'var(--entity-warning-color)';
          else if (player_state == 'paused')
            return 'var(--entity-minor-color)';
          else if (sensor_state == 'on')
            return 'var(--entity-youtube-color)';
          else
            return 'var(--state-icon-color)';
        ]]]
  card:
    - background-color: >
        [[[
          if (states['media_player.radio'].state == 'unknown'
              || states['media_player.radio'].state == 'unavailable')
            return 'var(--entity-button-critical-color)';
          else if (states['input_boolean.youtube_failed'].state == 'on')
            return 'var(--entity-button-severe-color)';
          else if (states['input_boolean.media_launcher_youtube'].state == 'on'
                && states['input_boolean.media_launcher_lock'].state == 'on')
            return 'var(--entity-button-active-color)';
          else if (states['input_boolean.media_launcher_youtube'].state == 'on')
            return 'var(--entity-button-selected-color)';
          else
            return 'var(--ha-card-background)';
        ]]]
tap_action:
  action: call-service
  service: input_boolean.toggle
  service_data:
    entity_id: input_boolean.media_launcher_youtube
double_tap_action:
  action: call-service
  service: homeassistant.turn_off
  service_data:
    entity_id:
      - media_player.youtube
      - input_boolean.youtube_failed
hold_action:
  action: call-service
  service: script.media_player_toggle
  service_data:
    media_player: media_player.youtube
