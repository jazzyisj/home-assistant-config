###############################################################################
## NWS Radar Popup
###############################################################################
type: custom:stack-in-card
cards:
  - type: iframe
    aspect_ratio: 100%
    url: !secret NWS_RADAR

  - type: entities
    entities:
      - type: buttons
        entities:
          - entity: sensor.dummy
            icon: mdi:radar
            name: NWS Radar
            tap_action:
              action: url
              url_path: !secret NWS_RADAR
        card_mod:
          style:
            hui-buttons-base $: |
              .ha-scrollbar {
                justify-content: center;
              }
