###############################################################################
## ZWave Devices
###############################################################################
type: glance
title: 'System Status'
icon: mdi:gauge-low
columns: 3
entities:
  - entity: sensor.hass_status
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        hide_header: true
        title: 'Hass Monitor'
        card: !include /config/lovelace/card/hass/hass_monitor.yaml
    card_mod:
      style: |
        :host {
          --paper-item-icon-color:
            {% set status = states('sensor.hass_status') %}
            {% if status == 'critical' %} var(--entity-critical-color)
            {% elif status == 'severe' %} var(--entity-severe-color)
            {% elif status == 'warning' %} var(--entity-warning-color)
            {% elif status == 'minor' %} var(--entity-minor-color)
            {% else %} var(--entity-ok-color)
            {% endif %}
          ;
        }

  - entity: sensor.network_status
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        hide_header: true
        title: 'Hass Monitor'
        card: !include /config/lovelace/card/network/network_monitor.yaml
    card_mod:
      style: |
        :host {
          --paper-item-icon-color:
            {% set status = states('sensor.network_status') %}
            {% if status == 'critical' %} var(--entity-critical-color)
            {% elif status == 'severe' %} var(--entity-severe-color)
            {% elif status == 'warning' %} var(--entity-warning-color)
            {% elif status == 'minor' %} var(--entity-minor-color)
            {% else %} var(--entity-ok-color)
            {% endif %}
          ;
        }

  - entity: sensor.zwave_status
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        hide_header: true
        title: 'Z-Wave Devices'
        card: !include /config/lovelace/card/hass/zwave_devices.yaml
    card_mod:
      style: |
        :host {
          --paper-item-icon-color:
            {% set status = states('sensor.zwave_status') %}
            {% if status == 'critical' %} var(--entity-critical-color)
            {% elif status == 'severe' %} var(--entity-severe-color)
            {% elif status == 'warning' %} var(--entity-warning-color)
            {% elif status == 'minor' %} var(--entity-minor-color)
            {% else %} var(--entity-ok-color)
            {% endif %}
          ;
        }

  - entity: sensor.pi_hole_status
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        hide_header: true
        title: 'Pi-Hole Monitor'
        card: !include /config/lovelace/card/network/pihole_monitor.yaml
    card_mod:
      style: |
        :host {
          --paper-item-icon-color:
            {% set status = states('sensor.pi_hole_status') %}
            {% if status == 'critical' %} var(--entity-critical-color)
            {% elif status == 'severe' %} var(--entity-severe-color)
            {% elif status == 'warning' %} var(--entity-warning-color)
            {% elif status == 'minor' %} var(--entity-minor-color)
            {% else %} var(--entity-ok-color)
            {% endif %}
          ;
        }

  - entity: sensor.server_status
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        hide_header: true
        title: 'Hass Monitor'
        card: !include /config/lovelace/card/system/server_monitor.yaml
    card_mod:
      style: |
        :host {
          --paper-item-icon-color:
            {% set status = states('sensor.server_status') %}
            {% if status == 'critical' %} var(--entity-critical-color)
            {% elif status == 'severe' %} var(--entity-severe-color)
            {% elif status == 'warning' %} var(--entity-warning-color)
            {% elif status == 'minor' %} var(--entity-minor-color)
            {% else %} var(--entity-ok-color)
            {% endif %}
          ;
        }
card_mod:
  class: basic
