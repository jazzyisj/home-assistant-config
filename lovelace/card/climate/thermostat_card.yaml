###############################################################################
## Thermostat
###############################################################################
type: thermostat
entity: climate.thermostat
card_mod:
  class: basic
  style: |
    ha-card {
      background:
        {% if is_state('sensor.thermostat_hvac_state','heat') %} var(--entity-heating-color)
        {% elif is_state('sensor.thermostat_hvac_state','cool') %} var(--entity-cooling-color)
        {% else %} var(--ha-card-background)
        {% endif %}
      ;
    }
