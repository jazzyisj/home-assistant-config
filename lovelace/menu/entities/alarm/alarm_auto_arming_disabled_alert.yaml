###############################################################################
## Alarm Auto Arming Disabled Alert Entity
###############################################################################
entity: alert.alarm_auto_arming_disabled
name: 'Alarm Disabled'
tap_action:
  action: none
double_tap_action:
  action: call-service
  service: input_boolean.turn_on
  service_data:
    entity_id: input_boolean.alarm_auto_arming
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.alarm_auto_arming_disabled
card_mod:
  style: |
    :host {
      --paper-item-icon-active-color: var(--entity-severe-color);
    }
