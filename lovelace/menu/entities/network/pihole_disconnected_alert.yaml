###############################################################################
## Pihole Disconnected Alert Entity
###############################################################################
entity: alert.pihole_disconnected
name: 'Pi-Hole Disconnected'
tap_action:
  action: none
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.pihole_disconnected
card_mod:
  style: |
    :host {
      --paper-item-icon-active-color: var(--entity-severe-color);
    }
