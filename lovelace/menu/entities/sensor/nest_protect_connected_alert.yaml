###############################################################################
## Nest Protect Connected Alert Entity
###############################################################################
entity: alert.nest_protect_connected
name: 'Nest Protect Disconnected'
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.nest_protect_connected
card_mod:
  style: |
    :host {
      --paper-item-icon-active-color: var(--entity-severe-color);
    }
