###############################################################################
## Google Traffic Connected Alert Entity
###############################################################################
entity: alert.google_traffic_disconnected
name: 'Google Traffic'
tap_action:
  action: none
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.google_traffic_disconnected
card_mod:
  style: |
    :host {
      --paper-item-icon-active-color: var(--entity-severe-color);
    }
