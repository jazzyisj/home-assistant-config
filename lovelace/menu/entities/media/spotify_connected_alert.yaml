###############################################################################
## Spotify Connected Alert Entity
###############################################################################
entity: alert.spotify_connected
name: 'Spotify'
tap_action:
  action: none
hold_action:
  action: call-service
  service: alert.turn_off
  service_data: alert.spotify_connected
card_mod:
  style: |
    :host {
      --paper-item-icon-active-color: var(--entity-warning-color);
    }
