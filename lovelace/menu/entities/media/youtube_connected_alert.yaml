###############################################################################
## Youtube Connected Alert Entity
###############################################################################
entity: alert.youtube_connected
name: 'Youtube'
tap_action:
  action: none
hold_action:
  action: call-service
  service: alert.turn_off
  service_data: alert.youtube_connected
card_mod:
  style: |
    :host {
      --paper-item-icon-active-color: var(--entity-warning-color);
    }
