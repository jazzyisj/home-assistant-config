type: custom:button-card
template: menu_button
entity: binary_sensor.spotify
name: Spotify
icon: mdi:spotify
styles:
  icon:
    - color: >
        [[[
          var player_state = states['media_player.spotify'].state;
          var switch_state = states['binary_sensor.spotify'].state;

          if (states['input_boolean.spotify_failed'].state == 'on'
              || states['binary_sensor.wan_connected_alert'].state == 'on')
            return 'var(--entity-critical-color)';
          else if (states['input_boolean.mute_all'].state == 'on')
            return 'var(--entity-warning-color)';
          else if (player_state == 'paused')
            return 'var(--entity-minor-color)';
          else if (switch_state == 'on')
            return 'var(--entity-spotify-color)';
          else
            return 'var(--state-icon-color)';
        ]]]
tap_action:
  action: call-service
  service: input_boolean.toggle
  service_data:
    entity_id: input_boolean.media_launcher_spotify
double_tap_action:
  action: call-service
  service: script.media_player_toggle_pause
  service_data:
    media_type: spotify
hold_action:
  action: call-service
  service: script.media_player_toggle
  service_data:
    media_player: media_player.spotify

