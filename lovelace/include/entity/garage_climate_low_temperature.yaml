entity: alert.garage_low_temperature
name: Garage Temp
tap_action:
  action: call-service
  service: browser_mod.popup
  service_data:
    deviceID: this
    title: Garage Settings
    large: false
    card: !include /config/lovelace/include/entities/garage_settings.yaml
double_tap_action:
  action: none
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.garage_low_temperature
style: |
  :host {
    --paper-item-icon-active-color: var(--entity-low-temp-color);
  }