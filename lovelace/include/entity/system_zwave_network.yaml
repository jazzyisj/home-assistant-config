entity: alert.system_zwave_network
tap_action:
  action: call-service
  service: browser_mod.popup
  service_data:
    deviceID: this
    title: Offline Zwave Devices
    large: false
    card:
      type: markdown
      content: !include /config/include/template/unavailable_zwave_device_markdown.yaml
double_tap_action:
  action: navigate
  navigation_path: /lovelace/system
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.system_zwave_network
style: |
  :host {
    --paper-item-icon-active-color: var(--entity-critical-color);
  }