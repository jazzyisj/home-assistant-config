entity: alert.presence_occupancy_mode
tap_action:
  action: call-service
  service: browser_mod.popup
  service_data:
    deviceID: this
    title: Occupancy Mode
    large: false
    card: !include /config/lovelace/include/entities/occupancy_mode.yaml
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.presence_occupancy_mode
style: |
  :host {
    --paper-item-icon-active-color: var(--entity-warning-color);
  }