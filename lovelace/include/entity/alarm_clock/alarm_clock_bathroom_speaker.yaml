type: custom:template-entity-row
entity: sensor.bathroom_speaker_next_alarm
name: "Bathoom Speaker"
icon: "{{ 'mdi:alarm-off' if is_state('sensor.bathroom_speaker_next_alarm','Off') else 'mdi:alarm' }}"
state: "{{ state_attr('sensor.bathroom_speaker_next_alarm','12hour') }}"
tap_action:
  action: fire-dom-event
  browser_mod:
    command: popup
    deviceID: this
    title: "Bathoom Speaker"
    card: !include /config/lovelace/include/card/alarm_clock/alarm_clock_bathroom_speaker.yaml
card_mod:
  style: |
    :host {
      --paper-item-icon-color:
        {% if is_state('sensor.bathroom_speaker_next_alarm','Off') %} var(--disabled-text-color)
        {% else %} var(--state-icon-active-color)
        {% endif %}
        ;
    }
