type: attribute
entity: sensor.alarm_clock_nap
attribute: 12hour
name: Nap
tap_action:
  action: fire-dom-event
  browser_mod:
    command: popup
    deviceID: this
    title: Nap Alarm Clock
    card: !include /config/lovelace/include/card/alarm_clock/alarm_clock_nap.yaml
double_tap_action:
  action: call-service
  service: input_boolean.toggle
  service_data:
    entity_id: input_boolean.alarm_clock_nap
hold_action:
  action: call-service
  service: switch.turn_off
  service_data:
    entity_id: switch.alarm_clock_nap
card_mod:
  style: |
    :host {
      --paper-item-icon-color:
        {% if is_state('sensor.alarm_clock_nap','Snooze') %} var(--alarm-clock-snooze-color)
        {% elif is_state('sensor.alarm_clock_nap','Active') %} var(--alarm-clock-active-color)
        {% elif is_state('sensor.alarm_clock_nap','Off') %} var(--state-icon-color)
        {% elif states('sensor.alarm_clock_nap')|lower in ['unknown','unavailable','none'] %} var(--entity-disabled-color)
        {% else %} var(--alarm-clock-pending-color)
        {% endif %}
        ;
    }