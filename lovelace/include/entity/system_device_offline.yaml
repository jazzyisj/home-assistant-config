entity: alert.system_device_offline
tap_action:
  action: call-service
  service: browser_mod.popup
  service_data:
    deviceID: this
    title: Offline Devices
    large: false
    card: !include /config/lovelace/include/entities/offline_devices.yaml
double_tap_action:
  action: navigate
  navigation_path: /lovelace/system
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.system_device_offline
style: |
  :host {
    --paper-item-icon-active-color: var(--entity-critical-color);
  }