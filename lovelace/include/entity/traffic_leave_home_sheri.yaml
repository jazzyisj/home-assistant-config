entity: alert.leave_home_sheri
name: Sheri Late
tap_action:
  action: call-service
  service: browser_mod.popup
  service_data:
    deviceID: this
    title: Schedule Settings
    large: false
    card: !include /config/lovelace/include/entities/work_schedule_settings.yaml
double_tap_action:
  action: none
hold_action:
  action: call-service
  service: alert.turn_off
  service_data:
    entity_id: alert.leave_home_sheri
style: |
  :host {
    --paper-item-icon-active-color: var(--entity-warning-color);
  }