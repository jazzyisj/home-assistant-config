type: entities
entities:
  - type: custom:template-entity-row
    icon: mdi:cog
    name: "Sprinkler Settings"
    card_mod:
      style: |
        :host {
          --paper-item-icon-color: var(--disabled-text-color);
        }
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        title: "Sprinkler Settings"
        card: !include /config/lovelace/include/card/sprinkler/sprinkler_settings.yaml
card_mod:
  class: basic
