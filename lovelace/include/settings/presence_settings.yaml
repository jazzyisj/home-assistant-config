type: entities
entities:
  - type: custom:dummy-entity-row
    entity: sensor.dummy
    icon: mdi:cog
    name: Presence Settings
    style: |
      :host {
        --paper-item-icon-color: var(--disabled-text-color);
      }
    tap_action:
      action: call-service
      service: browser_mod.popup
      service_data:
        deviceID: this
        title: Presence Settings
        large: false
        card: !include /config/lovelace/include/entities/presence_settings.yaml
