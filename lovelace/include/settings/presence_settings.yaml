type: entities
entities:
  - type: custom:template-entity-row
    icon: mdi:cog
    name: Presence Settings
    card_mod:
      style: |
        :host {
          --paper-item-icon-color: var(--disabled-text-color);
        }
    tap_action:
      action: fire-dom-event
      browser_mod:
        command: popup
        deviceID: this
        title: Presence Settings
        card: !include /config/lovelace/include/card/presence/presence_settings.yaml
card_mod:
  class: basic
