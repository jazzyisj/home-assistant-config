type: vertical-stack #BUGFIX
cards:
  - type: custom:stack-in-card #STACK #BUG entities card won't display if not nested in a vertical stack
    cards:
      - type: entities
        entities:
          - type: attribute
            entity: sensor.bathroom_speaker_next_alarm
            attribute: 12hour

          - entity: input_boolean.bathroom_speaker_alarm_enabled

          - type: custom:slider-entity-row
            entity: number.bathroom_speaker_alarm_volume
            name: Alarm Volume
            full_row: false
            toggle: true

          - entity: switch.bathroom_speaker_do_not_disturb
          - entity: sensor.bathroom_speaker_device
          - entity: sensor.bathroom_speaker_timers
          - entity: sensor.bathroom_speaker_alarms

          - type: custom:hui-element
            card_type: markdown
            content: >
              {%- if states('sensor.bathroom_speaker_alarms')|lower in ['','[]','unknown','unavailable','none'] -%}
                No alarms set.
              {%- else -%}
                {%- for item in state_attr('sensor.bathroom_speaker_alarms','alarms') -%}
                  [{{ item.status|upper }}] {{ item.fire_time|timestamp_custom('%a %B %d, %-I:%M %p',true,'ERROR') }} {%- if not loop.last -%}{{ '\n' }}{%- endif -%}
                {%- endfor -%}
              {%- endif -%}
            card_mod:
              class: sub_card