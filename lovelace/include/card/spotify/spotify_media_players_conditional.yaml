type: vertical-stack
cards:
  - type: custom:state-switch
    entity: template
    template: "{{ 'on' if states('media_player.spotify_hassio') in ['playing','paused'] else 'off' }}"
    states:
      'on': !include /config/lovelace/include/card/spotify/spotify_media_players_hassio.yaml

  - type: custom:state-switch
    entity: template
    template: "{{ 'on' if states('media_player.spotify_jazzyisj') in ['playing','paused'] else 'off' }}"
    states:
      'on': !include /config/lovelace/include/card/spotify/spotify_media_players_jazzyisj.yaml

  - type: custom:state-switch
    entity: template
    template: "{{ 'on' if states('media_player.spotify_sherigagnon') in ['playing','paused'] else 'off' }}"
    states:
      'on': !include /config/lovelace/include/card/spotify/spotify_media_players_sherigagnon.yaml