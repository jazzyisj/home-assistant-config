type: entities
entities:

  - type: custom:auto-entities
    filter:
      include:
        - entity_id: "sensor.*_device*"
    sort:
      method: name
      ignore_case: true
    card:
      type: custom:fold-entity-row
      head:
        entity: sensor.dummy
        name: Google Home Devices
        icon: mdi:timer-outline
        tap_action:
          action: none
      padding: 5

  - type: custom:auto-entities
    filter:
      include:
        - entity_id: "sensor.*_timers*"
    sort:
      method: name
      ignore_case: true
    card:
      type: custom:fold-entity-row
      head:
        entity: sensor.dummy
        name: Google Home Timers
        icon: mdi:timer-outline
        tap_action:
          action: none
      padding: 5

  - type: custom:auto-entities
    filter:
      include:
        - entity_id: "sensor.*_alarms*"
    sort:
      method: name
      ignore_case: true
    card:
      type: custom:fold-entity-row
      head:
        entity: sensor.dummy
        name: Google Home Alarms
        icon: mdi:timer-outline
        tap_action:
          action: none
      padding: 5

  - type: custom:auto-entities
    filter:
      include:
        - entity_id: "switch.*_do_not_disturb*"
    sort:
      method: name
      ignore_case: true
    card:
      type: custom:fold-entity-row
      head:
        entity: sensor.dummy
        name: Google Home DND
        icon: mdi:timer-outline
        tap_action:
          action: none
      padding: 5

card_mod:
  class: basic

