# only show active timers
type: entity-filter
state_filter:
  - 'active'
  #- 'idle'
show_empty: false
card:
  type: entities
entities:
  - entity: timer.radio_volume_wake
    name: Morning Volume Remaining
    style: |
      :host {
        --paper-item-icon-color: var(--paper-item-icon-active-color);
      }

  - entity: timer.radio_sleep
    name: Radio Sleep Timer Remaining
    style: |
      :host {
        --paper-item-icon-color: var(--paper-item-icon-active-color);
      }